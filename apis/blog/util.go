package blog

import "time"

func timestamp() time.Time {
	// now := time.Now().UTC().Unix()
	// return strconv.FormatInt(now, 10)
	// return int(now)

	// return time.Now().UTC()
	return time.Now()
}
